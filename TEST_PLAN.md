# Test Plan

## 1. 기능 테스트

### 1.1 FAQ 적재

- CSV 파일을 읽을 수 있다.
- 필수 컬럼이 없으면 오류를 표시한다.
- FAQ마다 `content`가 생성된다.
- embedding이 생성된다.
- Supabase `faqs` 테이블에 저장된다.
- 저장된 FAQ 개수가 입력 CSV와 일치한다.

### 1.2 유사도 검색

- FAQ와 유사한 질문을 입력하면 관련 FAQ가 반환된다.
- 관련 없는 질문은 결과가 없거나 낮은 유사도를 반환한다.
- `match_count`를 3으로 설정하면 최대 3개만 반환된다.
- `is_public = false`인 FAQ는 반환되지 않는다.

### 1.3 챗봇 API

- 질문이 비어 있으면 400 오류를 반환한다.
- 정상 질문에는 answer와 sources를 반환한다.
- 검색 결과가 없으면 근거 부족 메시지를 반환한다.
- OpenAI 또는 Supabase 오류가 발생하면 사용자 친화적인 오류를 반환한다.

### 1.4 UI

- 질문 입력 후 답변이 표시된다.
- 로딩 상태가 표시된다.
- 참고 FAQ가 표시된다.
- 예시 질문 버튼이 동작한다.
- 모바일에서 입력창과 답변이 잘 보인다.

## 2. RAG 품질 테스트

테스트 질문 예시:

| 질문 | 기대 결과 |
| --- | --- |
| AI를 처음 써도 수강할 수 있나요? | 교육 대상 FAQ를 근거로 답변 |
| 교육비가 얼마인가요? | 교육비 FAQ를 근거로 답변 |
| 결석하면 수료가 안 되나요? | 출석/수료 FAQ를 근거로 답변 |
| 노트북이 꼭 필요한가요? | 준비물 FAQ를 근거로 답변 |
| 주차장이 있나요? | FAQ에 없으면 근거 부족 답변 |

## 3. 보안 테스트

- 브라우저 번들에 `OPENAI_API_KEY`가 포함되지 않는다.
- 브라우저 번들에 `SUPABASE_SERVICE_ROLE_KEY`가 포함되지 않는다.
- 공개 클라이언트에서는 publishable/anon key만 사용한다.
- RLS가 활성화되어 있다.
- `is_public = false` 데이터가 API 결과에 나오지 않는다.

## 4. 교육 운영 테스트

- 수강생 계정으로 Supabase 프로젝트를 만들 수 있다.
- SQL을 복사해 실행할 수 있다.
- FAQ CSV 샘플을 열 수 있다.
- import 스크립트 실행 절차를 따라 할 수 있다.
- 최종 챗봇을 브라우저에서 실행할 수 있다.

## 5. 완료 체크리스트

- [ ] PRD 확인
- [ ] Notion FAQ 템플릿 준비
- [ ] Supabase SQL 적용
- [ ] 샘플 FAQ 20개 준비
- [ ] embedding 적재 성공
- [ ] 검색 함수 테스트 성공
- [ ] `/api/chat` 테스트 성공
- [ ] 챗봇 UI 테스트 성공
- [ ] 보안 키 노출 없음
- [ ] 강사용 시연 가능

