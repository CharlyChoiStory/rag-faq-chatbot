# Test Plan: 사내 규정집 RAG 챗봇

## 1. 기능 테스트

### 1.1 Notion 동기화

- `NOTION_API_KEY`가 설정되어 있다.
- `NOTION_ROOT_PAGE_ID=35e0d17ecf4a801d8166eff8982245a9`가 설정되어 있다.
- Notion integration이 사내 규정집 페이지에 초대되어 있다.
- `npm run sync:notion` 또는 `npm run sync:rules`가 실행된다.
- Supabase에 규정 청크가 저장된다.
- 각 청크에 page_title, page_url, content, embedding이 있다.

### 1.2 유사도 검색

- 규정과 유사한 질문을 입력하면 관련 규정이 반환된다.
- 다른 표현으로 질문해도 관련 규정을 찾는다.
- 관련 없는 질문은 근거 부족으로 처리한다.
- `match_count`를 5로 설정하면 최대 5개 근거만 반환된다.
- keyword fallback이 한국어 핵심어를 보완한다.

### 1.3 챗봇 API

- 질문이 비어 있으면 400 오류를 반환한다.
- 정상 질문에는 answer와 sources를 반환한다.
- 검색 결과가 없으면 근거 부족 메시지를 반환한다.
- OpenAI/Supabase/Notion 오류가 발생하면 사용자 친화적인 오류를 반환한다.
- 답변이 검색 근거 밖으로 벗어나지 않는다.

### 1.4 카카오톡 스타일 UI

- 사용자 말풍선이 오른쪽에 표시된다.
- AI 말풍선이 왼쪽에 표시된다.
- 하단 입력창이 모바일에서 잘 보인다.
- 로딩 중 “규정집을 확인하고 있어요...” 같은 메시지가 표시된다.
- 참고 규정 카드가 답변 아래에 표시된다.
- Notion 원문 링크가 열리거나 복사 가능하다.

## 2. 대표 질문 테스트

| 질문 | 기대 결과 |
| --- | --- |
| 연차는 며칠 전까지 신청해야 해? | 연차/휴가 규정 근거로 신청 기한 안내 |
| 반차도 사용할 수 있어? | 반차 또는 휴가 규정 근거 안내 |
| 출장비는 언제까지 정산해야 해? | 출장/비용 정산 규정 근거 안내 |
| 재택근무 신청 절차 알려줘. | 재택근무 규정 근거 안내 |
| 회사 노트북을 분실하면 어떻게 해야 해? | 자산/보안 규정 근거 안내 |
| 개인정보 자료는 어떻게 보관해야 해? | 개인정보/보안 규정 근거 안내 |
| 복리후생비 사용 기준 알려줘. | 복리후생 규정 근거 안내 |
| 결재 승인 절차는 어떻게 돼? | 결재/승인 규정 근거 안내 |
| 지각하면 어떻게 처리돼? | 근태 규정 근거 안내 |
| 점심 메뉴 추천해줘. | 규정집 근거 부족 답변 |

## 3. RAG 품질 기준

- Top 5 검색 결과 안에 정답 규정이 포함되면 통과
- 답변에 출처가 1개 이상 있으면 통과
- 근거 없는 질문에 추측하지 않으면 통과
- 대표 질문 10개 중 8개 이상 통과를 MVP 기준으로 한다.

## 4. 보안 테스트

- 브라우저 번들에 `OPENAI_API_KEY`가 포함되지 않는다.
- 브라우저 번들에 `SUPABASE_SERVICE_ROLE_KEY`가 포함되지 않는다.
- 브라우저 번들에 `NOTION_API_KEY`가 포함되지 않는다.
- `.env.local`은 Git에 추적되지 않는다.
- 내부 규정 데이터가 공개 저장소에 커밋되지 않는다.
- 배포 시 접근 제한 필요 여부를 확인한다.

## 5. 명령어 검증

```bash
npm run lint
npm run build
npm run sync:notion
npm run sync:rules
npm run test:rules-search
npm run check:rules
git status --short
```

주의: sync/test 명령은 OpenAI/Supabase/Notion API를 실제 호출할 수 있다.

## 6. 완료 체크리스트

- [ ] 문서가 사내 규정집 챗봇 기준으로 업데이트됨
- [ ] Notion integration 권한 확인
- [ ] Supabase 스키마 적용
- [ ] 규정 청크 동기화 성공
- [ ] 대표 질문 검색 성공
- [ ] 답변 출처 표시
- [ ] 카카오톡 스타일 UI 적용
- [ ] API key 노출 없음
- [ ] `npm run lint` 통과
- [ ] `npm run build` 통과
- [ ] 시연 가능

## 7. 생성형 AI PoC 테스트

### 7.1 mode 선택 테스트

- UI에서 `규정 질문`, `이메일 작성`, `공지문 작성`, `회의록 요약`, `아이디어 생성`을 선택할 수 있다.
- 선택한 mode가 API 요청 body에 포함된다.
- 알 수 없는 mode가 들어오면 안전하게 `qa`로 처리한다.

### 7.2 생성 기능 대표 테스트

| mode | 질문 | 기대 결과 |
| --- | --- | --- |
| qa | 연차는 며칠 전까지 신청해야 해? | 규정 근거 기반 답변과 출처 |
| email | 연차 신청 절차를 직원에게 안내하는 이메일을 작성해줘. | 제목/본문/마무리가 있는 이메일 초안 |
| notice | 보안 교육 참석 안내 공지문을 작성해줘. | 임직원 대상 공지문 초안 |
| meeting-summary | 회의록 텍스트를 붙여넣고 요약 요청 | 핵심 요약/결정사항/액션 아이템 |
| ideation | 보안 규정을 쉽게 알리는 캠페인 아이디어 5개 | 실행 가능한 아이디어와 주의사항 |

### 7.3 품질 기준

- 규정 관련 숫자, 기한, 절차를 임의로 만들지 않는다.
- 이메일/공지문은 업무 문서 형식으로 바로 복사해 쓸 수 있다.
- 회의록 요약은 입력에 없는 담당자/일정을 만들지 않는다.
- 아이디어는 사실과 제안을 구분한다.
- 기존 규정 Q&A 기능이 회귀하지 않는다.
