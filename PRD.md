# PRD: 사내 규정집 RAG AI 챗봇 웹앱

작성일: 2026-05-12

## 1. 프로젝트 개요

본 프로젝트는 hsikchoi@gmail.com 계정의 Notion에 있는 “사내 규정집” 노트를 기반으로, 회사 규정 정보를 자연어로 쉽게 검색하고 질의응답할 수 있는 사내 업무 지원 AI 챗봇 웹앱을 만드는 것을 목표로 한다.

대상 Notion 원본:
https://www.notion.so/charly-choi/35e0d17ecf4a801d8166eff8982245a9?pvs=12

현재 Notion 노트에는 약 20개의 회사 규정 데이터가 있으며, 이 데이터를 RAG 검색 대상으로 사용한다.

## 2. 문제 정의

회사 규정은 문서로 존재하더라도 직원이 필요한 조항을 빠르게 찾기 어렵다. 특히 휴가, 근태, 출장비, 결재, 보안, 복리후생, 개인정보 등 업무 상황별 질문은 자연어로 묻는 경우가 많다.

이 프로젝트는 다음 문제를 해결한다.

- 직원이 규정 문서를 일일이 열어 검색해야 한다.
- 질문 표현과 규정 제목/조항명이 달라 검색이 잘 안 된다.
- AI가 규정집 근거 없이 일반론을 답하면 위험하다.
- 신규 입사자나 관리자가 반복 질문에 계속 대응해야 한다.
- Notion에 있는 규정집을 웹 챗봇으로 쉽게 활용하고 싶다.

## 3. 목표

- Notion 사내 규정집을 Supabase Vector DB에 동기화한다.
- 직원이 자연어로 질문하면 관련 규정을 검색한다.
- AI가 검색된 규정 근거 안에서만 답변한다.
- 답변에 참고 규정 제목, 조항, Notion 링크를 표시한다.
- 카카오톡 스타일 UI로 누구나 쉽게 사용할 수 있게 한다.
- VSCode + Codex가 이어서 구현할 수 있도록 문서와 명령어를 정리한다.

## 4. 비목표

- 법률/노무 자문 자동화
- 인사 시스템, ERP, 그룹웨어 실시간 연동
- 직원별 권한/로그인 완성형 구축
- 규정 승인 워크플로우 구현
- Slack/KakaoTalk 실제 메신저 연동
- 규정 변경 자동 승인/배포

## 5. 대상 사용자

- 사내 규정을 찾아야 하는 임직원
- 신규 입사자
- 인사/총무/관리 담당자
- 사내 업무 지원 챗봇을 시연하려는 운영자

## 6. 핵심 사용자 시나리오

### 6.1 직원

1. 웹 챗봇에 접속한다.
2. “연차는 며칠 전까지 신청해야 해?”, “출장비 정산 기한은?”처럼 자연어로 질문한다.
3. 챗봇이 관련 규정 조항을 검색한다.
4. AI가 규정 근거를 바탕으로 간단명료하게 답변한다.
5. 직원은 참고 규정 링크를 눌러 원문을 확인한다.

### 6.2 관리자

1. Notion 사내 규정집 노트를 수정한다.
2. 동기화 명령을 실행한다.
3. Supabase에 최신 규정 청크와 embedding이 저장된다.
4. 테스트 질문으로 검색 품질을 확인한다.

## 7. 주요 기능

### 7.1 Notion 규정집 동기화

- `NOTION_ROOT_PAGE_ID` 기준으로 루트 페이지와 하위 페이지/블록 수집
- 각 규정 제목, 본문, URL, 청크 인덱스 저장
- 기존 청크와 upsert
- 변경 후 남은 오래된 청크 정리

### 7.2 규정 문서 청킹

- 규정 제목을 청크 앞에 포함
- 조항/항목 단위가 깨지지 않도록 줄 단위 분할
- 기본 청크 크기: 500~900자
- 검색 정확도를 위해 규정명, 카테고리, 키워드가 content에 포함되도록 설계

### 7.3 RAG 검색

- 사용자 질문을 embedding으로 변환
- Supabase pgvector 유사도 검색
- 한국어 자연어 질문을 위한 키워드 보완 검색
- 상위 3~5개 근거만 답변 생성에 사용
- threshold 이하 결과는 근거 부족 처리

### 7.4 답변 생성

- 검색된 규정 근거 안에서만 답변
- 확정적이지 않은 내용은 담당 부서 확인 권장
- 답변 형식:
  - 요약 답변
  - 적용 기준/절차
  - 주의사항
  - 참고 규정

### 7.5 카카오톡 스타일 UI

- 모바일 우선 채팅 화면
- 사용자 말풍선: 오른쪽, 노란색 계열
- AI 답변 말풍선: 왼쪽, 흰색/연회색 계열
- 하단 고정 입력창
- 예시 질문 버튼
- 로딩 중 말풍선
- 답변 아래 출처 카드

## 8. 기술 요구사항

- Frontend: Next.js 15, React 19, TypeScript
- Styling: Tailwind CSS 또는 기존 CSS 유지 후 카카오톡 스타일로 개편
- Backend: Next.js API Route
- Database: Supabase Postgres
- Vector Search: pgvector
- Source: Notion API
- Embedding: OpenAI `text-embedding-3-small`
- Answer Generation: OpenAI Responses API 또는 Chat Completions

## 9. 보안 요구사항

- `OPENAI_API_KEY`, `NOTION_API_KEY`, `SUPABASE_SERVICE_ROLE_KEY`는 서버에서만 사용한다.
- `.env.local`은 Git에 커밋하지 않는다.
- 브라우저에는 service role key를 절대 노출하지 않는다.
- 규정집이 내부 자료라면 배포 전 인증/접근 제한을 추가한다.
- 답변 로그를 저장할 경우 개인정보/민감정보 포함 여부를 고려한다.

## 10. 성공 기준

- Notion 사내 규정집의 약 20개 규정 데이터가 Supabase에 저장된다.
- 대표 질문 10개 중 8개 이상이 관련 규정 근거를 찾는다.
- 답변이 규정집 내용을 벗어나지 않는다.
- 근거가 부족한 질문에는 모른다고 답한다.
- 답변에 참고 규정 출처가 표시된다.
- UI가 카카오톡 스타일로 자연스럽게 보인다.
- `npm run lint`와 `npm run build`가 통과한다.

## 11. 주요 리스크와 대응

| 리스크 | 영향 | 대응 |
| --- | --- | --- |
| Notion API 권한 부족 | 동기화 실패 | integration을 대상 페이지에 초대하고 root page id 확인 |
| 규정 문서 구조가 불규칙함 | 검색 품질 저하 | 제목/조항/키워드가 청크에 포함되도록 정리 |
| 일반론 답변 발생 | 규정 신뢰도 저하 | 시스템 프롬프트에서 근거 외 답변 금지 |
| 민감한 내부 규정 노출 | 보안 사고 | 배포 전 로그인/접근 제한 추가 |
| threshold가 너무 높거나 낮음 | 검색 누락/오답 | 테스트 질문으로 0.35~0.65 범위 조정 |

## 12. MVP 완료 정의

- `npm run sync:notion`으로 사내 규정집이 동기화된다.
- `/api/notion-chat` 또는 `/api/rules-chat`이 질문에 답변한다.
- 메인 화면이 사내 규정 챗봇 단일 목적에 맞게 바뀐다.
- 카카오톡 스타일 말풍선 UI가 적용된다.
- 출처 카드에서 Notion 원문 링크를 확인할 수 있다.

## 13. 생성형 AI PoC 추가 요구사항

사내 규정 검색 챗봇을 기반으로 기본 생성형 AI 업무 기능을 추가한다.

### 13.1 추가 기능 범위

- 규정 Q&A: 기존 RAG 답변 유지
- 이메일 작성: 규정 근거 기반 업무 이메일 초안 생성
- 공지문 작성: 임직원 대상 공지/안내문 초안 생성
- 회의록 요약: 사용자가 입력한 회의록/메모의 핵심 요약, 결정사항, 액션 아이템 추출
- 아이디어 생성: 사내 규정/업무 맥락을 참고한 개선 아이디어 제안

### 13.2 기능별 답변 정책

- 규정 Q&A는 근거 없는 답변을 금지한다.
- 이메일/공지문은 문체와 형식은 생성할 수 있지만, 규정상 사실은 검색 근거를 따라야 한다.
- 회의록 요약은 사용자가 입력한 내용 안에서만 요약하고, 없는 담당자/일정은 만들지 않는다.
- 아이디어 생성은 “확인된 사실”과 “제안 아이디어”를 구분한다.
- 규정과 충돌할 가능성이 있는 아이디어는 주의 표시를 붙인다.

### 13.3 PoC 성공 기준

- UI에서 작업 유형을 선택할 수 있다.
- API 요청에 `mode`가 전달된다.
- mode별 프롬프트와 temperature가 다르게 적용된다.
- 규정 기반 생성 결과에는 가능한 경우 출처가 표시된다.
- `npm run lint`, `npm run build`가 통과한다.
